@AGENTS.md

## Claude Code

- Deploy `dist/worker_agent/wrangler.json`, not source `wrangler.jsonc`.
- Flue workflow/agent schema slots are valibot; append DO migrations — never rewrite existing tags.
- Path-scoped rules: `.claude/rules/worker-agent.md` when editing agents/workflows.
