Answer the following question as a single, self-contained report.

Populate the structured result:

- `answer`: the full synthesized answer, in Markdown.
- `sources`: every citation you relied on (lorem ipsum reference id), each with
  a short supporting excerpt where available. Leave it empty only if no source
  could be found.

Question:
